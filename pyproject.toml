[project]
name = "denia-agent-toolkit"
version = "0.1.0"
description = "Local automation toolkit for graphics and engine workflows."
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "pillow>=12.2.0",
    "pyautogui>=0.9.54",
    "pywinctl>=0.4.1",
]
